// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseURL: "http://192.168.0.159:4200/v1"
  // baseURL:"http://192.168.0.155:4000/v1"
  // baseURL: "http://192.168.0.133:8002/v1", //local //pargat
  // baseURL:"https://lmn-app-nov22.tradesl.com/v1", //live
  // baseURL: "https://lmn-redswitches.shinedezign.pro/v1" //dev
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
